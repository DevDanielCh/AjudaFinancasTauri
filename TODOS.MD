- [x] ajustar os sorts das tables que não estão funcionando corretamente, eles não estão ordenando nada. Quando clicar para fazer a ordenação, deve fazer uma nova query no banco de dados conforme a ordenação pedida
- [] na tela de dashboards, criar um novo dashboard com a meta de investimento do mes, essa meta deve ser configuravel na tela de configurações, essa config deve ser uma porcentagem e essa porcentagem será calculada em cima do valor total de entradas do mês, e o valor da meta será exibido no dashboard e mostrar se bateu ou não a meta de investimento
- [] quero dividir os menus do sistema em módulos, o primero módulo é simplesmente a organização financeira do usuario e o outro módulo é a parte de investimentos, então quero que o menu seja dividido em dois menus, um chamado "Organização Financeira" e outro chamado "Investimentos", e dentro de cada menu, colocar os itens correspondentes a cada módulo
- [] para fazer o item de cima, creio que devemos revalidar nossa organização de pastas e arquivos do projeto, vamos criar uma pasta para cada módulo dentro do src e dentro de cada modulo termos as pastas de Models, Repositories, Services, Controllers e Views, e dentro de cada pasta termos os arquivos correspondentes a cada módulo, assim teremos uma melhor organização do projeto
  - [] com isso, cada Form de cadastro/edição/visualizacao deve ficar dentro de viem e todos devem ter a mesma nomenclatura, então por exemplo, para o módulo de organização financeira, teremos a pasta src/OrganizacaoFinanceira/Views e dentro dela teremos as pastas de cada entidade, por exemplo, src/OrganizacaoFinanceira/Views/Transaction, e dentro dessa pasta teremos os arquivos de cadastro, edição e visualização da entidade Transaction, com a nomenclatura TransactionAddForm, ContasEditForm e ContasViewForm, e assim por diante para cada entidade de cada módulo, mas essa organização tem que ser feita tanto para o backend em Tauri como o frontend em Next
  